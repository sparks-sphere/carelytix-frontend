# Force npm usage
package-manager=npm

# Legacy peer deps for compatibility
legacy-peer-deps=true

# Prefer offline for faster builds
prefer-offline=true

# Ensure consistent package manager
engine-strict=true 